<UserControl x:Class="stu_profo.View.CourseItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Width="660" Height="Auto">
    <Border Background="White" CornerRadius="10" Margin="2" BorderBrush="Transparent" BorderThickness="1">
        <Grid>


            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="20,0">
                <Ellipse Width="48" Height="48" Fill="LightGray" Margin="0,0,10,0"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding CourseName}" FontSize="14" FontWeight="Light"/>
                    <TextBlock Text="{Binding CourseCode}" FontSize="7" FontWeight="ExtraLight"/>
                    <TextBlock Text="{Binding CourseID}" FontSize="11" FontWeight="Bold"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10,0,40,15">
                <TextBlock Text="GPA" FontSize="11" FontWeight="Bold" Foreground="Gray" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBlock Text="{Binding GPA, StringFormat='{}{0:N1}'}" FontSize="11" FontWeight="Bold" Foreground="Gray" VerticalAlignment="Center"/>
            </StackPanel>

            <Button Content="Select" Background="White" Foreground="#1a4e96" BorderBrush="LightBlue" BorderThickness="1" VerticalAlignment="Top" HorizontalAlignment="Right" Width="66" Margin="0,24,20,0">
                <Button.Template>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5" Padding="5,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</UserControl>
